#include <stdio.h>

/* Multiline comment. int argc tells how many arguments to take. char *argv[] tells it to take inifinite chars */
int main(int argc, char *argv[]) {
    int distance = 100;

	// Single line comment
	printf("You are %d miles away.\n", distance);

	return 0;
}
